import { ConnectionCredentialPipe } from './connection-credential.pipe';

describe('ConnectionCredentialPipe', () => {
  it('should be defined', () => {
    expect(new ConnectionCredentialPipe()).toBeDefined();
  });
});
